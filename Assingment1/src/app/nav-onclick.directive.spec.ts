import { NavOnclickDirective } from './nav-onclick.directive';

describe('NavOnclickDirective', () => {
  it('should create an instance', () => {
    const directive = new NavOnclickDirective();
    expect(directive).toBeTruthy();
  });
});
